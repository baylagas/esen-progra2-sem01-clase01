function showMessage(message)
{
    /* crear un mensaje en pantalla que reciba una cadena message y concatenarlo con */
    /* el nombre balbino */
    var teacherName = "balbino";
    var finalMessage = message + teacherName
    alert(finalMessage);
}